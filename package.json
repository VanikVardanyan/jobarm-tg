{
  "name": "jobbarm",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@9.1.1",
  "engines": { "node": ">=20.6" },
  "scripts": {
    "dev:server": "pnpm --filter server dev",
    "test": "pnpm --filter server test"
  }
}
